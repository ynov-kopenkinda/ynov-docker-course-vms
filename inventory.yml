all:
  vars:
    ansible_user: ubuntu
  children:
    group1:
      hosts:
        # Configured on https://www.duckdns.org
        yogurt.trouve-ton-indien.store:
      vars:
        domain_name: yogurt.trouve-ton-indien.store

